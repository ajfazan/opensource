PROJECT( UTILITY )
CMAKE_MINIMUM_REQUIRED( VERSION 2.8.4 )

SET( HEADERS algorithm.hpp mapped_memory.hpp )

SET( CMAKE_INSTALL_PREFIX $ENV{WS_INSTALL} )

INSTALL( FILES ${HEADERS} DESTINATION include/utility )
